# Placeholder

ssh.php
Helper file containing calls to phpseclib, PHP library that offers SSH and SCP actions via PHP. We use it to restart DHCP service (from pxeclients.php) as well as for assorted actions in manage.php (reboot, fetching screenshots, etc).
